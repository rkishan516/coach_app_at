import 'package:flutter/material.dart';
import 'dart:ui' as ui;

class XD_AndroidMobile33 extends StatelessWidget {
  XD_AndroidMobile33({
    Key key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xff000000),
      body: Stack(
        children: <Widget>[],
      ),
    );
  }
}
